# Your NestJS backend
VITE_API_BASE_URL=http://localhost:3000

# Supabase - used only for Realtime messaging on the frontend.
# Auth goes through the backend's /auth endpoints, not directly from here.
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
